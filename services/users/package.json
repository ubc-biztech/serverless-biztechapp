{
  "name": "biztechapi-users",
  "version": "1.0.0",
  "dependencies": {
    "uuid": "^8.3.1"
  },
  "scripts": {
    "itest": "npm run itest users --prefix ../../",
    "utest": "npm run utest users --prefix ../../",
    "test": "npm run utest users --prefix ../../ && npm run itest users --prefix ../../"
  }
}
