{
  "name": "biztechapi-transactions",
  "version": "1.0.0",
  "scripts": {
    "itest": "npm run itest transactions --prefix ../../",
    "utest": "npm run utest transactions --prefix ../../",
    "test": "npm run utest transactions --prefix ../../ && npm run itest transactions --prefix ../../"
  }
}