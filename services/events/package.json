{
  "name": "biztechapi-events",
  "version": "1.0.0",
  "scripts": {
    "itest": "npm run itest events --prefix ../../",
    "utest": "npm run utest events --prefix ../../",
    "test": "npm run utest events --prefix ../../ && npm run itest events --prefix ../../"
  },
  "devDependencies": {
    "serverless-offline": "^13.6.0",
    "webpack-cli": "^5.1.1"
  }
}
