{
  "name": "biztechapi-profiles",
  "version": "1.0.0",
  "scripts": {
    "itest": "npm run itest profiles --prefix ../../",
    "utest": "npm run utest profiles --prefix ../../",
    "test": "npm run utest profiles --prefix ../../ && npm run itest profiles --prefix ../../"
  }
}