{
  "name": "biztechapi-hello",
  "version": "1.0.0",
  "dependencies": {},
  "scripts": {
    "itest": "npm run itest hello --prefix ../../",
    "utest": "npm run utest hello --prefix ../../",
    "test": "npm run utest hello --prefix ../../ && npm run itest hello --prefix ../../"
  }
}
